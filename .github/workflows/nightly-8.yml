---
name: "nightly-8"
on:
    schedule:
        - cron: '42 5 * * *'
    workflow_dispatch:

jobs:
    nightly-8:
        name: "dispatch-nightly-8"
        uses: ./.github/workflows/ci.yml
        with:
            ref: '8'
